# i3blocks config file
#
# Please see man i3blocks for a complete reference!
# The man page is also hosted at http://vivien.github.io/i3blocks
#
# List of valid properties:
#
# align
# color
# command
# full_text
# instance
# interval
# label
# min_width
# name
# separator
# separator_block_width
# short_text
# signal
# urgent

# Global properties
#
# The top properties below are applied to every block, but can be overridden.
# Each block command defaults to the script name to avoid boilerplate.
# Change $SCRIPT_DIR to the location of your scripts!
command=$HOME/.config/i3blocks/$BLOCK_NAME/$BLOCK_NAME
markup=none
separator_block_width=30

[time]
command=echo -n "🇮🇳 "; TZ='Asia/Kolkata' date "+%a %d-%b(%m) %H:%M"
interval=5

[time]
command=echo -n "🇺🇸 "; TZ='America/Los_Angeles' date "+%a %d-%b(%m) %H:%M"
interval=5

[time]
command=echo -n "🇳🇱 "; TZ='Europe/Amsterdam' date "+%a %d-%b(%m) %H:%M"
interval=5
